<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Tenant Routes (intentionally empty)
|--------------------------------------------------------------------------
|
| This app runs in single-database mode with a single central API (routes/api.php).
| Tenants are resolved from the authenticated user, not from a domain, so there are
| no domain-scoped tenant routes. TenancyServiceProvider still loads this file if it
| exists; leaving it empty is intentional.
|
*/
